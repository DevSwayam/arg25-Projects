[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = [
  "cd unwallet-bond-credit 2>/dev/null || true",
  "npm install"
]

[start]
cmd = "cd unwallet-bond-credit 2>/dev/null || true && npm run start"

